<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>Gotta Catch 'em All</title>
    </head>
    <body>
        <h1>Pokemon  Page</h1>
            {% for item in data limit: 5 %}
                <img src="{{ item.img }}"> <br />
                Name: <a href="/pokemon/{{ forloop.index0 }}">{{ item.name }}</a><br />
                Type: {{ item.type | join: ", "}} <br />
                Stats:  
                HP: {{ item.stats.hp }} &nbsp;
                Attack: {{ item.stats.attack }} &nbsp;
                Defense: {{ item.stats.defense }} &nbsp; 
                <p><a href="/pokemon/{{ forloop.index0 }}/edit">Edit</a></p>
                <form action="/pokemon/{{ forloop.index0 }}?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE" />
                </form>
            {% endfor %}
        <nav>
            <a href="/pokemon/new">Add a New Pokemon</a>
        </nav>
    </body>
</html>
